#include <stdio.h>

int main() {
    int a,b, countOfDivision;
    printf("Podaj a:\n");
    scanf("%d",&a);
    printf("Podaj b:\n");
    scanf("%d",&b);

    for (int i = 0; i <= b; i++) {
        for (int j = 2; countOfDivision < 2; j++) {
            if(i % j == 0) {
                countOfDivision++;
            }
            if countOfDivision
        }

    }
}